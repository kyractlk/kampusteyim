import 'package:image_picker/image_picker.dart';

Future<XFile?> pickWebFile({
  required String accept,
  required String fallbackName,
}) async {
  throw UnsupportedError('Web file pick yalnızca web’de kullanılabilir');
}
